<div class="container">
  <div class="row">
    <div class="col-md-5">
      {{#each burger}} {{#unless devoured}}
      <li class="list-group-item list-group-item-info">{{id}}. {{burger_name}}</li>
      <button data-id="{{this.id}}" class="devour btn btn-primary">Devoure It!!!</button>
      {{/unless}} {{/each}}
    </div>
    <div class="col-md-2"></div>
    <div class="col-md-5">

      {{#each burger}} {{#if this.devoured}}

      <li class="list-group-item list-group-item-info">{{this.id}}. {{this.burger_name}}</li>
      {{/if}} {{/each}}

    </div>
  </div>
</div>
</div>


<div class="container">
  <div class="row">
    <div class="col-md-3">

    </div>
    <div class="col-md-6">
<h1 class="label">Add Buger</h1>
<form class="create-form">

  <div class="form-group"><br>
    <label for="ca" style="color:white">Burger Name:</label>
    <br>
    <input type="text" class="txt2" id="ca" name="name">
    <br>
  </div>

<br>
  <button type="submit" class="btn btn-primary">Add Burger</button>
</form>
    </div>
    <div class="col-md-3">

    </div>
  </div>
</div>
</div>



